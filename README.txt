Introduction
============

This package, when installed via Plone site setup add-ons, overrides the view template for plone.app.contenttypes' file type. Clicking on the displayed link will use the @@display-file view rather than @@download for (and only for) PDFs.